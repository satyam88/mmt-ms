apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-mmt-ingress
  namespace: dev
  labels:
    env: dev
    app: mmt-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: dev-mmt.connectingclouds.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: dev-mmt-service
                port:
                  number: 8080